version: "0.2.1"
release_date: "2026-02-07"
breaking: true
description: |
  Introduces mandatory seed manifest ingestion, seed provenance fields, and
  documentation impact enforcement in the Trinity loop.

changes:
  - type: paradigm_shift
    description: "Introduce seed manifest schema and shared spec/common/seed_manifest.json for mandatory seed order."
    migration:
      action: auto
      operations:
        - add_file

  - type: add_field
    step_id: "00_charter"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "01_capabilities"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "02_system_sketch"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "02a_delivery_baseline"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "03_glossary"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "04_fr_list"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "05_interface_contracts"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "06_invariants"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "07_nfrs"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "08_fixtures"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "09_impl_plan"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "10_governance"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "11_redteam"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "12_ci_gates"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "13_extension_generator"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "13a_completeness_assessment"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "14_roadmap"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "15_scaffold"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md
  - type: add_field
    step_id: "16_impl_context"
    path: "seed_refs"
    required: true
    migration:
      action: ai_assisted
      prompt: template_add_field.md

  - type: add_constraint
    step_id: "16_impl_context"
    path: "context_evaluation"
    description: "Trinity loop prompts must evaluate additional context (README maps, tooling docs) and update seed_manifest when required."
    migration:
      action: ai_assisted

  - type: add_constraint
    step_id: "16_impl_context"
    path: "docs_impact.strict"
    description: "Spec changes (including seed_manifest) are treated as non-doc changes and require docs_impact to be required."
    migration:
      action: ai_assisted

  - type: add_constraint
    step_id: "16_impl_context"
    path: "prompts/16a-16c"
    description: "Align Trinity loop prompts with schema fields and enforced run_specdev wrapper."
    migration:
      action: ai_assisted

  - type: add_field
    step_id: "16_impl_context"
    path: "plan.docs_impact"
    required: false
    migration:
      action: auto
      operations:
        - add_field

  - type: add_field
    step_id: "seed_manifest"
    path: "docs_policy.readme_depth_default"
    required: true
    migration:
      action: auto
      operations:
        - add_field

  - type: add_field
    step_id: "seed_manifest"
    path: "docs_policy.readme_depth_by_scope"
    required: true
    migration:
      action: auto
      operations:
        - add_field
