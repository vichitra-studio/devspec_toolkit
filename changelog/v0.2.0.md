# v0.2.0 - 2026-01-17

## Added
- **Schema**: `plan.tasks` removed — implementation now under `checklist[].implementation`
- **Schema**: `step_id` removed — use `id` only (redundant identity field)
- **Schema**: `metadata` replaced by typed `extensions` (e.g., `extensions.review_state`, `extensions.execution_context`)
- **Schema**: `spec_ref` changed from string to structured object `{ type, id, line_range, commit_hash }`
- **Schema**: `spec_ref.commit_hash` and `line_range` are now **REQUIRED** for all spec references
- **Prompts**: All three implementation prompts (16a Planner/16b Coder/16c Reviewer) rewritten to align with v2.1 schema and eliminate hallucination vectors

- **Step 00 Hardening**:
    - **Strict Schema**: Enforced `minItems: 3` for scope and required fields for `success_metrics` (measurement_method) and `stakeholders` (needs).
    - **Structured Seeds**: Updated `seed_overview.md` to prompt for specific metrics (Unit, Target) and Owner Team.
    - **Traceability**: Added explicit tracing instructions to `prompt_00` to ingest constraints from `seed_tech_stack.md`.
    - **Validation**: Added `minItems: 1` to `assumptions` and `risks` to preventing empty/lazy lists.
- **Step 01 Hardening**:
    - **Prompt Schema Reference**: Fixed broken `errorState` reference in embedded schema (`.../core/collections/1#errorState` → `.../core/errors/1#errorState`).
    - **Owner Enum Alignment**: Updated `prompt_01_capabilities.md` to support full owner list from `atoms.schema.json` (added `product`, `business`, `engineering`).
    - **Hallucination Prevention**: Added explicit note that System Sketch references are optional if not yet available.
- **Step 03 Hardening (Glossary)**:
    - **Schema Constraints**: Added `minItems: 1` to `terms`, `minLength: 2` to `term`, `minLength: 20` to `definition`, `minLength: 1` and pattern validation to `domain` and `units`. Refactored common fields (`id`, `owner`, `created_at`) to use Core Atoms.
    - **Tooling / Verification**: Added `verify_step_03.py` for fixture validation and enhanced `matrix.py` with Step 03 integrity checks (including FR noun coverage validation).
    - **Test Fixtures**: Added comprehensive positive and negative test fixtures for glossary validation.
    - **Prompt Updates**: Updated `prompt_03_glossary.md` with aligned owner enum, negative constraints, conditional context, and hardening schema reference.
    - **Documentation**: Added review report and troubleshooting documentation for Step 03.
- **Step 02 Hardening (System Sketch)**:
    - **Schema Constraints**: Enforced `components.minItems: 1`, `responsibilities` 3–6 items, required `owner`, required `trace_refs` on components and connections, `connections` required when multiple components exist, `trust_boundary` required on connections, `auth` + structured `rate_limit` required for `partner`/`public`, `reliability` required for `event`, `schema_ref` pattern validation, and a controlled tag enum.
    - **Rate Limits**: Replaced free-text `rate_limit` with structured object (`rps`, `burst`, `window_s`, `scope`) and bounds, including custom `burst >= rps` validation.
    - **Traceability**: Standardized capability links to `trace_refs` with `type: doc` and enforced capability coverage in tooling.
    - **Prompt Updates**: Clarified dependency order, added negative constraints, defined external components, clarified output rules, and aligned embedded schema with hardening changes.
    - **Tooling**: Added `verify_step_02.py` and matrix checks for component uniqueness, connection integrity, schema_ref format, capability coverage, and external trust-boundary rules.
    - **Fixtures**: Added positive and negative fixtures covering responsibilities bounds, trust boundaries, schema refs, rate limits (including `burst < rps`), trace refs, capability coverage, and external integration boundaries.
- **Step 02a Hardening (Delivery Baseline)**:
    - **Schema Constraints**:
        - **Empty Shell Prevention**: Enforced `minProperties: 1` on environment objects to prevent "lazy" empty definitions.
        - **Gate Naming Strictness**: Enforced `kebab-case` regex (`^[a-z0-9-]+$`) on `ci_gates` to ensure machine-readability and consistent CLI mapping.
        - **Traceability**: Added optional `trace` field to link delivery infrastructure to upstream capabilities or charter.
    - **Tooling**: Added `verify_step_02a.py` including a new `--self-test` mode for verifying the fixture suite.
    - **Test Fixtures**: Established a formal regression suite with `valid_minimal.json` (baseline) and negative tests for empty environments (`invalid_empty_env.json`) and invalid gate patterns (`invalid_gate_format.json`).
    - **Prompt Updates**:
        - **Schema Sync**: Updated embedded schema to reflect new `trace` and `minProperties` rules.
        - **Hallucination Removal**: Deleted irrelevant copy-paste instructions about "preconditions" to focus purely on delivery deliverables.
        - **Negative Constraints**: Added explicit "Do not output TBD" and "Do not include secret values" constraints.
- **Step 04 Hardening (Functional Requirements)**:
    - **Schema Constraints**: Enforced `statement` quality (`minLength: 20`) and `acceptance_criteria` rigor (`minLength: 15`).
    - **Traceability**: Fixed the "Broken Bridge" by allowing `traceRef` to point to `capability` in `collections.schema.json`.
    - **Tooling**: Added `verify_step_04.py` for comprehensive fixture validation and specific negative test case support.
    - **Integrity**: Updated `matrix.py` to enforce `FR -> Capability` trace validation, closing the loop on Step 01 alignment.
    - **Prompt Updates**: Added explicit structure for trace objects, negative constraints (no loose criteria, no implementation details), and fully aligned owner enum.
- **Part 1: Schema Hardening (v2.1)**:
    - **Critical Gaps Fixed**:
        - Ambiguity enforcement: strict enums for `source` (spec/code/plan/mixed/review) and `severity` (blocking/non_blocking)
        - Conditional mitigation: non-blocking ambiguities require `mitigation` field with `minLength: 10`
        - Active checklist validation: active items require minimum 1 action (`minItems: 1`)
    - **Evidence Hardening**:
        - `evidence_binding` object with `timestamp` (ISO 8601), `sha256` (64-char hash), `exit_code` (0-255), optional `command`
        - Evidence minimum length: `minLength: 20` characters
        - Passed status pattern validation: regex `(PASSED|passed|OK|SUCCESS|✓|0 (errors|failures?|failed)|\d+ passed)`
        - Evidence directory convention documented: `evidence/{step_id}/{command_sha256}.log`
    - **Checklist Integrity**:
        - Verified status gating: `implementation.status: verified` requires `evidence` on all actions
        - Active checklist requirements: non-deferred items must have `implementation` block
        - Checklist status enum: added `checklist_status` with values `active` (default) or `deferred`
    - **Type Tightening**:
        - Checklist enums: strict enums for `type` (behavior/constraint/validation/metadata/perf/logging/docs) and `layer` (db/model/service/api/integration/tests/docs/config)
        - Action type conditionals: file operations require `target`, run_command requires `command`
        - Drift schedule constraint: pattern validation for cron expressions and named intervals
        - Structured test commands: support both string and object formats with `expected_exit_code`, `timeout_seconds`, `description`
        - Existing structures citation: enhanced with `signature`, `source_file`, `line_range` fields
    - **Review Hardening**:
        - Finding severity requirements: `blocking` or `major` findings require `remediation_task`
        - Verdict/CI consistency: `verdict: verified` requires `fixture_status.ci_status: green`
    - **DRY Optimization**:
        - Extracted $defs: created reusable definitions for `specRef`, `severityLevel`, `executionStatus`, `evidenceObject`
    - **New Additions**:
        - Coverage tracking: `coverage_status` object with `total`, `verified`, `deferred`, `pending` counters
        - Scope validation: `scope_validation` with `in_scope`, `out_of_scope` arrays and `acknowledged` boolean (required when out_of_scope has items)
        - Scope validation: `scope_validation` with `in_scope`, `out_of_scope` arrays and `acknowledged` boolean (required when out_of_scope has items)
    - **Step 05 Hardening (Interface Contracts)**:
        - **Schema Enrichment**: Added `parameters` array to API object for explicit modeling of Query, Path, and Header parameters.
        - **Prompt Fixes**: Resolved critical bug in embedded schema `errorState` reference (pointing to `core/errors/1`).
        - **Prompt Sync**: Synchronized embedded schema to include new `parameters` definition.
        - **Prompt Hardening**: Added explicit trace object structure, protocol usage instructions (gRPC/MQTT), expanded `owner` enum, and negative constraints.
        - **Tooling**: Added `verify_step_05.py` for automated schema and fixture validation.
        - **Fixtures**: Added `valid_rest_api.json` (positive) and `invalid_bad_protocol.json` (negative) test cases.
- **Step 06 Hardening (Invariants)**:
    - **Schema Constraints**: Enforced `trace` as mandatory (`required: ["trace"]`) to prevent orphaned rules. All invariants must now link to an upstream logic (FR/NFR).
    - **Prompt Hardening**:
        - **Owner Alignment**: Synced `owner` enum with core v0.2.0 (`product`, `business`, `engineering`).
        - **Negative Constraints**: Added explicit "DO NOT" section forbidding "text" language unless necessary and banning skipped traces.
        - **Embedded Schema**: Synchronized the prompt's embedded reference with the actual schema to prevent hallucinations.
    - **Test Fixtures**: Added comprehensive suite (`tests/fixtures/step_06/`):
        - `valid_full.json`: Reference implementation with usage of `jsonlogic` and `cel`.
        - `invalid_missing_trace.json`: Negative test validating the new mandatory trace requirement.
        - `invalid_owner.json`: Negative test validating the strict owner enum.
    - **Tooling**: Added `verify_step_06.py` for automated schema and fixture verification, serving as the canonical integrity check.
- **Step 07 Hardening (Non-Functional Requirements)**:
    - **Schema Constraints**:
        - **Quantitative Enforcement**: `target` field now requires digit presence (regex `^.*\d+.*$`) to prevent purely qualitative values like "fast".
        - **Strict IDs**: `nfr_id` forced to match `^nfr-[a-z0-9]+-[a-z0-9-]+$` pattern (e.g. `nfr-latency-p95`).
    - **Test Fixtures**: Established comprehensive test suite `tests/fixtures/step_07/` including `valid_full.json` (reference implementation) and negative tests for pattern violations.
    - **Tooling**:
        - **New Validator**: Added `tools/verify_step_07.py` script for standalone NFR fixture verification.
        - **Tracing Matrix**: Enhanced `tools/specdev_tools/matrix.py` to support direct `NFR -> FR` tracing, ensuring accurate coverage reporting for non-functional constraints.
    - **Prompt Hardening**:
        - **Owner Alignment**: Synced `owner` enum with Core Atoms, adding `product`, `business`, `engineering`.
        - **Negative Constraints**: Explicitly banned "invented owners" and "qualitative targets" in a new `## FORBIDDEN ACTIONS` section.
        - **Traceability Guidance**: Added strict instructions on how to trace critical NFRs to Upstream FRs or Components.
- **Step 08 Hardening (Test Fixtures)**:
    - **Schema Constraints**:
        - **Strict Traceability**: `targets` field is now **REQUIRED** and must be non-empty (`minItems: 1`).
        - **Typed Targets**: Enforced strict `traceRef` structure via linter validation.
    - **Prompt Hardening**:
        - **Negative Constraints**: Forbidden actions section explicitly bans invented IDs, placeholders, and markdown commentary.
        - **Mode Guidance**: Clarified `contract` mode requirements (must include `status`, `body`, and optional `headers`) vs `unit`/`e2e`.
        - **Anti-Hallucination**: Added "Self-Correction" phase to verify ID existence before emitting JSON.
    - **Tooling**:
        - **Enhanced Linter**: `fixtures_lint.py` now loads and validates targets against `04_fr_list` (FRs), `06_invariants` (Rules), and `07_nfrs` (NFRs), closing the traceability loop.
        - **Verification Script**: Added `verify_step_08.py` with comprehensive valid/invalid test suites and unique ID checks.
        - **Test Infrastructure**: Organized fixtures into `valid/` and `invalid/` directories with mock context for isolated testing.
- **Step 09 Hardening (Implementation Plan)**:
    - **Schema Constraints**:
        - **Structured Tech Stack**: Replaced simple string arrays to typed objects (`name`, `version`, `rationale`) under `tech_stack`. This enables precise version pinning and automated scaffold generation.
        - **Traceability**: Added specific `trace` field to schema to link Plan to Charter (`minItems: 1`).
        - **Milestone Traceability**: Added `deliverables` field to `milestones` to link execution steps directly to FRs or APIs (`fr-*`, `api-*`).
    - **Prompt Hardening**:
        - **Capabilities Check**: Added explicit Operating Flow instruction to cross-check `tech_stack` against Step 1 (`spec/01_capabilities.json`), forbidding unauthorized tech.
        - **Negative Constraints**: Banned "Unstructured Tech Stacks", "Orphan Milestones" (must trace to FRs), and "Generic Versions" (must use semantic constraints).
    - **Tooling**:
        - **New Validator**: Added `tools/verify_step_09.py` for comprehensive schema and fixture validation.
        - **Matrix Support**: Updated `matrix.py` to scan `deliverables` for dependency tracking, integrating Step 9 into the full project trace matrix.
        - **Test Fixtures**: Established `tests/fixtures/step_09/` with `valid_complete.json`, `valid_minimal.json`, and negative test cases for format and missing fields.
- **Step 10 Hardening (Governance)**:
    - **Schema Constraints**:
        - **Traceability**: Added `trace` and `links` fields to enable linking governance policies to upstream requirements.
        - **Validation**: Enforced strict `spec_first_policy` (boolean) and `commit_message_rules` structure.
        - **Enum Enforcement**: Restricted `pr_rules` to a strict enum of allowed CI/Toolkit actions (e.g., `validate`, `test`, `lint`) to prevent free-form strings.
    - **Tooling**:
        - **Fixes**: Patched `governance.py` to correctly identify governance files by ID or filename, fixing a critical fail-open bug.
        - **Verification**: Added `verify_step_10.py` for comprehensive schema and regex validation, including full support for the `owner` enum (e.g., `product`, `business`).
    - **Test Fixtures**: Established `tests/fixtures/step_10/` with valid policies and negative test cases for regex, schema violations, and **invalid enum values** (`invalid_pr_rules_enum.json`).
    - **Prompt Hardening**:
        - **Negative Constraints**: Added strict prohibitions against text-only rules and defined specific anti-patterns.
        - **Schema Sync**: Aligned embedded schema with the new `trace`-enabled definition.
- **Step 11 Hardening (Red-Team)**:
    - **Schema Constraints**:
        - **Critical Traceability**: Added `target_ids` property (array of `traceRef`) to `threat` objects, mandating links to specific APIs or Components.
        - **Structured Mitigations**: Converted `mitigations` from string array to object array (`type`, `id`, `note`), enabling direct linking to Invariants (`inv-*`) and NFRs (`nfr-*`).
        - **Categorization**: Enforced strict `category` enum (`authn`, `authz`, `business_logic`, `transport`, `data_privacy`) for better reporting.
    - **Prompt Hardening**:
        - **Rewritten Role**: Redefined role as "Senior Security Architect", enforcing adversarial thinking and strict schema compliance.
        - **Validation Rules**: Added explicit checks for target linkage and valid enum usage.
        - **Embedded Schema**: Updated to reflect the v2.1 schema with `target_ids` and structured mitigations.
    - **Tooling**:
        - **Enhanced Verification**: `verify_step_11.py` now indexes 7 distinct artifact types (APIs, Components, FRs, NFRs, Invariants, Capabilities, Fixtures) to validate all cross-references in threats and mitigations.
    - **Test Fixtures**: Established `tests/fixtures/step_11/` with `valid_full.json` (real IDs) and invalid cases for category and target validation.
- **Step 12 Hardening (CI Gates)**:
    - **Schema Constraints**:
        - **Strict Step Structure**: Converted `steps` from simple string array to structured objects (`id`, `name`, `command`) to warn against hallucinated tools.
        - **Traceability**: Added `trace` array to root (using standard `traceRef` collection), enabling direct linkage to Governance (`10-governance`) or Delivery Baseline (`02a-delivery`).
        - **DAG Validation**: Schema now supports explicit `requires` for DAG definition, with `minItems: 1` enforcement where populated.
    - **Prompt Hardening**:
        - **Schema Synchronization**: Updated embedded schema to match v2.1 definition (trace array, structured steps), eliminating potential for model drift.
        - **Negative Constraints**: Explicitly banned "TBD" placeholders, "invented tools" that do not map to `specdev_tools`, and unstructured prose.
        - **Tooling Context**: Added explicit list of available `specdev_tools` CLI commands to ground the model's generation in reality.
    - **Tooling**:
        - **New Validator**: Developed `tools/verify_step_12.py` to enforce Directed Acyclic Graph (DAG) integrity (cycle detection, missing dependencies) and command structure.
        - **Integration**: Registered `12_ci_gates.schema.json` in `tools/schema_registry.json`, enabling standard validation via `specdev-tools validate`.
    - **Documentation**:
        - **Troubleshooting**: Added verified "Step 12 Troubleshooting" guide to `docs/developers/workflows/discovery.md` covering cycles, types, and common errors.
        - **Developer Reference**: Updated `reference.md` with usage patterns for the new verification scripts.
    - **Test Fixtures**: Established `tests/fixtures/step_12/` with `valid_dag.json` (reference), `cycle_dag.json` (cycle detection), and `missing_dep.json` to verify the new validator.
- **Step 13 & 13a Hardening (Extension & Completeness)**:
    - **Critical Fix**: Resolved "Split Brain" naming collision between Step 13a (Assessment) and 13a (Extension) by forcing extensions to use `ext_[0-9]{2}_[topic].json` convention.
    - **Schema Constraints**:
        - **Strict Regex**: Enforced `^ext-[0-9]{2}-[a-z0-9-]+$` for IDs and strict file naming patterns.
        - **Completeness Logic**: Step 13a schema now strictly validates `specification_source` to prevent hallucinations.
    - **Prompt Hardening**:
        - **Negative Constraints**: Added "Do NOT invent trivial extensions" rules.
        - **Mandatory Ingestion**: Step 13a now explicitly reads `13_extension_manifest.json` to check for missing files.
    - **Tooling**:
        - **New Validator**: Added `tools/verify_step_13.py` to validate manifest structure and verify physical existence of referenced extensions.
        - **Matrix Integration**: Updated `matrix.py` to ingest the extension manifest and trace into extension files.
    - **Test Fixtures**: Added complete suite `tests/fixtures/step_13/` covering valid manifests, valid extensions, and invalid naming/missing file scenarios.
- **Step 14 Hardening (Roadmap)**:
    - **Schema**:
        - **Structured Tasks**: `milestones[].tasks` now uses objects `{ task_id, description, status? }` with kebab-case IDs and a multi-word description pattern.
        - **Task Acceptance Criteria**: Optional `acceptance_criteria` objects add `{ criterion_id, text, fixture_ref? }` with minimum text length.
        - **Structured Dependencies**: `dependencies[]` now uses objects `{ type, id, owner?, note? }` with `type` enum (`milestone`/`external`) and required `owner` for external dependencies.
        - **Dependency Rationale**: External dependencies require a multi-word `note` explaining rationale/impact.
        - **Required Dates**: `milestones[].target_date` is required and enforced by schema.
        - **Deliverables**: `milestones[].deliverables` are arrays of `traceRef` objects (no string deliverables).
        - **Migration Plan**: `migration_plan` is a non-empty string (schema-level minimum length).
        - **Milestone Traceability**: `milestones[].source_milestones` required to map back to Step 09 milestone IDs.
    - **Prompt Updates** (`prompts/prompt_14_roadmap.md`):
        - **Output Rules**: Require top-level `$schema` and clarify that validation strips it.
        - **Field Guidance**: Added/expanded guidance for `tasks`, `acceptance_criteria`, `dependencies`, `source_milestones`, and `migration_plan` (kebab-case IDs, external owner + note requirement, length guidance).
        - **Embedded Schema Sync**: Embedded schema now mirrors structured tasks/dependencies, acceptance criteria, source_milestones, and required `target_date`.
        - **Output Contract**: Updated example with `$schema`, `migration_plan`, task objects, and source_milestones.
    - **Tooling** (`tools/verify_step_14.py`):
        - **Custom Checks**: Date sequencing, task object validation (shape + unique `task_id`), acceptance criteria quality, dependency object validation + rationale, source_milestones presence, migration plan length enforcement.
        - **Schema vs Custom**: Dependency format and enum errors now primarily handled by schema validation.
    - **Fixtures** (`tests/fixtures/step_14/`):
        - **Valid**: `valid_roadmap.json`, `valid_roadmap_migration.json`.
        - **Invalid**: `invalid_tech_mismatch.json`, `invalid_missing_target_date.json`, `invalid_missing_source_milestones.json`, `invalid_date_order.json`, `invalid_dependency_format.json`, `invalid_dependency_missing_note.json`, `invalid_task_format.json`, `invalid_task_acceptance_criteria.json`, `invalid_status_enum.json`, `invalid_risk_status_enum.json`, `invalid_migration_plan.json`.
    - **Documentation**:
        - Updated `docs/audit/step_14_deep_dive.md` and `docs/audit/step_14_hardening_plan.md` to reflect structured tasks/dependencies, new validations, and fixture coverage.
    - **Referential Integrity**:
        - **Cross-Spec Validation**: The verification tool now accepts `--step-09-spec` to validate that `source_milestones` in the roadmap actually map to real upstream milestone IDs.
        - **Tech Stack Consistency**: Enforced that the Roadmap's tech stack exactly matches the Implementation Plan's definitions and versions, preventing "drift" where the roadmap introduces unapproved technologies.
- **Step 15 Hardening (Scaffold Generation)**:
    - **Schema Constraints**:
        - **Traceability Bridge**: Added `trace` and `links` fields to finally connect the Scaffold to Step 05 (Interface Contracts), fixing the "Broken Bridge".
        - **Strict Validation**: Made `validators` and `build_status` **REQUIRED** fields to prevent "lazy" generations.
        - **Method Enums**: Restricted `route_map.method` to standard HTTP verbs (GET, POST, etc.) via strict enum, banning invalid methods like "GETTER".
    - **Prompt Hardening**:
        - **Anti-Hallucination**: Removed phantom instructions asking for "preconditions" and "error states" that the schema didn't support.
        - **Logic Fixes**: Explicitly instructed the AI to default `build_status` to `pending` (preventing hallucinated "green" builds) and forbade guessing.
        - **Negative Constraints**: Added strict "DO NOT" rules against duplicating API refs or inventing non-existent modules.
    - **Tooling**:
        - **New Validator**: Created `tools/verify_step_15.py` to enforce schema compliance, `api_ref` uniqueness, and kebab-case naming conventions.
    - **Test Fixtures**: Established `tests/fixtures/step_15/` with valid baseline fixtures and negative test cases for method enums, missing validators, and duplicate routes.
- **Part 2: Prompt System Hardening**:
    - **Schema-Prompt Alignment**:
        - 16a Planner: removed embedded v1 schema (L154-267), replaced with v2.1 reference
        - 16b Coder: updated from task-iteration to requirement-iteration mental model
        - 16c Reviewer: added `metadata_usage` rating, aligned with mandatory `spec_ref` requirements
    - **Prompt Architecture Updates** (Zero Inference, Evidence Binding, Negative Constraints):
        - Checklist-Driven Architecture: "one checklist item = one testable behavior"
        - Zero Inference Principle: explicit prohibition against guessing or assuming implementation details
        - Evidence binding: mandatory mechanical proof for all claims with verbatim output requirements
        - Negative constraints: comprehensive "FORBIDDEN ACTIONS" sections in all three prompts
    - **16a Planner v2.0**:
        - Replaced `plan.tasks` documentation with `checklist[].implementation` architecture
        - Added structured `spec_ref` object requirements (type, id, line_range, commit_hash)
        - Removed deprecated `coding_patterns`, replaced with `coding_examples` array
        - Added atomicity requirements: one checklist item per behavior, one action per file edit
        - Added negative constraints: no hallucinated `existing_structures`, no placeholder commit hashes, no untyped metadata
    - **16b Coder v2.0**:
        - Changed operating flow: "Read Plan → Iterate Tasks" to "Read Checklist → Fill Implementation Slots → Verify"
        - Added evidence requirements: verbatim stdout/stderr (max 50 lines), no paraphrasing, full tracebacks
        - Added scope enforcement: files must match `target_file_patterns` or raise `emergent_ambiguity`
        - Added dependency verification: check package installation, config file existence, service health
        - Added negative constraints: no TODOs, no placeholder implementations, no test output modification
    - **16c Reviewer v2.0**:
        - Added evidence-based audit checklist (5-point verification protocol)
        - Enhanced verdict rules with strict gating: no `verified` with red CI, no `verified` without evidence
        - Added red flags detection: empty evidence, paraphrased evidence, scope violations
        - Added negative constraints: mandatory spot-checking, no vague findings, no scope creep approval

## Changed
- **Tooling**: `specdev validate` now validates against `16_impl_context_v2.schema.json`
- **Schema**: `evidence` in `execution_results` is now a string (min 20 chars), while `evidence_binding` holds metadata (sha256, timestamp, exit_code)
- **Prompts**: All three implementation prompts (16a/16b/16c) completely rewritten for v2.1 schema
- **Prompts**: Embedded schema snippets updated from v1 to v2.1 structure across all prompts

## Removed
- **Schema**: `coding_patterns` (deprecated, use `coding_examples` array)
- **Schema**: `plan.tasks` (replaced by `checklist[].implementation`)
- **Schema**: `step_id` field (redundant with `id`)
- **Schema**: Untyped `metadata` dumps (replaced by structured `extensions`)

## Fixed
- **Prompts**: Fixed critical bug in Coder (16b) where `evidence` object was not populated for verified actions
- **Documentation**: Restored missing `schema_hardening_v21_unified.md`
- **Schema**: Fixed `evidence` conditional requirement — now only required when `status: verified`
