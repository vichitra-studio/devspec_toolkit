{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:02a-delivery-baseline",
  "title": "02a_delivery_baseline",
  "allOf": [
    {
      "$ref": "vc:core:step-base",
      "description": "Inherits shared step-base properties ($schema, id, owner, created_at, canonical_refs_used)."
    },
    {
      "type": "object",
      "properties": {
        "trace": {
          "description": "Traceability links back to upstream artifacts (charter, system sketch).",
          "type": "array",
          "items": {
            "$ref": "vc:core:collections#traceRef",
            "description": "A traceability reference for the delivery baseline."
          }
        },
        "environments": {
          "description": "Map of deployment environments (dev, ci, staging, prod) with their configurations.",
          "type": "object",
          "propertyNames": {
            "$ref": "vc:core:collections#environmentName",
            "description": "Environment name constraint: must be a valid environment (dev, ci, staging, prod)."
          },
          "additionalProperties": {
            "$ref": "vc:core:collections#environmentConfig",
            "description": "Environment configuration values for a specific deployment environment."
          },
          "required": [
            "dev",
            "ci",
            "staging",
            "prod"
          ],
          "minProperties": 4
        },
        "ci_gates": {
          "description": "List of CI gate names that must pass before deployment (e.g., 'lint', 'unit-tests').",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "A CI gate identifier in kebab-case format.",
            "type": "string",
            "pattern": "^[a-z0-9-]+$"
          }
        },
        "secrets": {
          "description": "List of secret names or keys required by the delivery pipeline.",
          "$ref": "vc:core:collections#stringArray"
        },
        "compliance": {
          "description": "List of compliance standards or frameworks the delivery must satisfy (e.g., 'SOC2', 'GDPR').",
          "$ref": "vc:core:collections#stringArray"
        },
        "environment_ref": {
          "description": "Canonical reference to the environment definitions in the canonical registry.",
          "$ref": "vc:core:collections#canonicalRef"
        },
        "command_ref": {
          "description": "Canonical reference to the command definitions used in CI gates.",
          "$ref": "vc:core:collections#canonicalRef"
        },
        "policy_ref": {
          "description": "Canonical reference to the compliance policies governing this delivery baseline.",
          "$ref": "vc:core:collections#canonicalRef"
        }
      },
      "required": [
        "environments",
        "ci_gates"
      ],
      "description": "Step-specific properties for 02a_delivery_baseline."
    }
  ],
  "unevaluatedProperties": false
}
