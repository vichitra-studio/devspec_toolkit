{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:01-capabilities",
  "title": "01_capabilities",
  "allOf": [
    {
      "$ref": "vc:core:step-base",
      "description": "Inherits shared step-base properties ($schema, id, owner, created_at, canonical_refs_used)."
    },
    {
      "type": "object",
      "properties": {
        "capabilities": {
          "description": "Array of system capabilities derived from the charter, each with scope and traceability.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "A single capability describing what the system can do, with inputs, outputs, and error states.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "capability_id": {
                "description": "Unique kebab-case identifier for this capability (e.g., 'cap-user-login').",
                "$ref": "vc:core:atoms#kebabId"
              },
              "verb": {
                "description": "Action verb describing the capability (e.g., 'authenticate', 'create').",
                "type": "string",
                "minLength": 2
              },
              "description": {
                "description": "Human-readable explanation of what this capability does.",
                "type": "string"
              },
              "scope": {
                "description": "Whether this capability is in-scope, out-of-scope, or planned for future delivery.",
                "type": "string",
                "enum": [
                  "in",
                  "out",
                  "future"
                ]
              },
              "owner": {
                "description": "Team or domain responsible for implementing this capability.",
                "$ref": "vc:core:atoms#owner"
              },
              "inputs": {
                "description": "Data or signals required to exercise this capability.",
                "$ref": "vc:core:collections#stringArray"
              },
              "outputs": {
                "description": "Data or effects produced when this capability executes successfully.",
                "$ref": "vc:core:collections#stringArray"
              },
              "preconditions": {
                "description": "Conditions that must be true before this capability can execute.",
                "$ref": "vc:core:collections#stringArray"
              },
              "postconditions": {
                "description": "Conditions guaranteed to be true after successful execution of this capability.",
                "$ref": "vc:core:collections#stringArray"
              },
              "error_states": {
                "description": "Possible error conditions that may arise during capability execution.",
                "type": "array",
                "items": {
                  "$ref": "vc:core:errors#errorState",
                  "description": "An error state that may occur during capability execution."
                }
              },
              "trace": {
                "description": "Traceability links back to upstream artifacts (e.g., charter segments, stakeholders).",
                "type": "array",
                "items": {
                  "$ref": "vc:core:collections#traceRef",
                  "description": "A traceability reference for this capability."
                }
              },
              "capability_ref": {
                "description": "Canonical reference linking this capability to the canonical registry.",
                "$ref": "vc:core:collections#canonicalRef"
              },
              "action_ref": {
                "description": "Canonical reference to the action verb used in this capability.",
                "$ref": "vc:core:collections#canonicalRef"
              },
              "entity_ref": {
                "description": "Canonical reference to the primary entity this capability operates on.",
                "$ref": "vc:core:collections#canonicalRef"
              },
              "role_ref": {
                "description": "Canonical reference to the role that exercises this capability.",
                "$ref": "vc:core:collections#canonicalRef"
              }
            },
            "required": [
              "capability_id",
              "verb",
              "scope",
              "capability_ref",
              "trace"
            ]
          }
        }
      },
      "required": [
        "capabilities"
      ],
      "description": "Step-specific properties for 01_capabilities."
    }
  ],
  "unevaluatedProperties": false
}
