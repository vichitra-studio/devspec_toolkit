{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:06-invariants",
  "title": "06_invariants",
  "allOf": [
    {
      "$ref": "vc:core:step-base",
      "description": "Inherits shared step-base properties ($schema, id, owner, created_at, canonical_refs_used)."
    },
    {
      "type": "object",
      "properties": {
        "rules": {
          "description": "Array of system invariant rules that must always hold true, expressed as formal or textual constraints.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "A single invariant rule with its formal expression, scope, severity, and traceability.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "inv_id": {
                "description": "Unique kebab-case identifier for this invariant (e.g., 'inv-balance-non-negative').",
                "$ref": "vc:core:atoms#kebabId"
              },
              "description": {
                "description": "Human-readable description of what this invariant enforces.",
                "type": "string"
              },
              "language": {
                "description": "Expression language used: jsonlogic (machine-evaluable), cel (Common Expression Language), or text (human-readable).",
                "type": "string",
                "enum": [
                  "jsonlogic",
                  "cel",
                  "text"
                ]
              },
              "expression": {
                "description": "The formal or textual expression of this invariant in the specified language.",
                "type": "string"
              },
              "scope": {
                "description": "The components and APIs to which this invariant applies.",
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "components": {
                    "description": "List of component IDs this invariant applies to.",
                    "$ref": "vc:core:collections#kebabIdArray"
                  },
                  "apis": {
                    "description": "List of API IDs this invariant applies to.",
                    "$ref": "vc:core:collections#kebabIdArray"
                  }
                }
              },
              "severity": {
                "description": "Violation severity: 'warn' for advisory violations, 'error' for blocking violations.",
                "type": "string",
                "enum": [
                  "warn",
                  "error"
                ]
              },
              "trace": {
                "description": "Traceability links back to upstream requirements, APIs, or components.",
                "type": "array",
                "items": {
                  "$ref": "vc:core:collections#traceRef",
                  "description": "A traceability reference for this invariant rule."
                }
              },
              "policy_ref": {
                "description": "Canonical reference to the governance policy this invariant enforces.",
                "$ref": "vc:core:collections#canonicalRef"
              },
              "risk_category_ref": {
                "description": "Canonical reference to the risk category this invariant mitigates.",
                "$ref": "vc:core:collections#canonicalRef"
              },
              "status_ref": {
                "description": "Canonical reference to the lifecycle status of this invariant.",
                "$ref": "vc:core:collections#canonicalRef"
              }
            },
            "required": [
              "inv_id",
              "description",
              "language",
              "expression",
              "scope",
              "trace",
              "policy_ref"
            ]
          }
        }
      },
      "required": [
        "rules"
      ],
      "description": "Step-specific properties for 06_invariants."
    }
  ],
  "unevaluatedProperties": false
}
