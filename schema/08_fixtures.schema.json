{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:08-fixtures",
  "title": "08_fixtures",
  "allOf": [
    {
      "$ref": "vc:core:step-base",
      "description": "Inherits shared step-base properties ($schema, id, owner, created_at, canonical_refs_used)."
    },
    {
      "type": "object",
      "properties": {
        "fixtures": {
          "description": "Array of test fixtures providing concrete input/expected pairs for validating spec artifacts.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "A single test fixture with input data, expected output, testing mode, and target references.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "fixture_id": {
                "description": "Unique kebab-case identifier for this fixture (e.g., 'fix-login-success').",
                "$ref": "vc:core:atoms#kebabId"
              },
              "description": {
                "description": "Human-readable description of what this fixture tests.",
                "type": "string"
              },
              "targets": {
                "description": "Array of trace references to the spec artifacts this fixture validates (FRs, APIs, invariants, NFRs).",
                "type": "array",
                "minItems": 1,
                "items": {
                  "$ref": "vc:core:collections#traceRef",
                  "description": "A trace reference to a spec artifact this fixture validates."
                }
              },
              "mode": {
                "description": "Testing mode: unit (isolated), contract (API boundary), e2e (end-to-end flow), or redteam (adversarial).",
                "$ref": "vc:core:atoms#testingMode"
              },
              "input": {
                "description": "Input data for this test fixture, can be any valid JSON value.",
                "$ref": "vc:core:collections#anyJson"
              },
              "expected": {
                "description": "Expected output or result for this test fixture, can be any valid JSON value.",
                "$ref": "vc:core:collections#anyJson"
              },
              "tags": {
                "description": "Classification tags for organizing and filtering fixtures.",
                "type": "array",
                "items": {
                  "$ref": "vc:core:atoms#tag",
                  "description": "A classification tag for this fixture."
                }
              },
              "tag_ref": {
                "description": "Canonical reference linking fixture tags to the canonical registry.",
                "$ref": "vc:core:collections#canonicalRef"
              }
            },
            "required": [
              "fixture_id",
              "mode",
              "input",
              "expected",
              "targets",
              "tag_ref"
            ]
          }
        }
      },
      "required": [
        "fixtures"
      ],
      "description": "Step-specific properties for 08_fixtures."
    }
  ],
  "unevaluatedProperties": false
}
