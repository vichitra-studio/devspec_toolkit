{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:seed-manifest",
  "title": "seed_manifest",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema URI identifying the schema this seed manifest conforms to."
    },
    "seed_manifest_id": {
      "$ref": "vc:core:atoms#kebabId",
      "description": "Unique kebab-case identifier for this seed manifest instance."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Semantic version of the seed manifest format."
    },
    "created_at": {
      "$ref": "vc:core:atoms#timestamp",
      "description": "ISO 8601 timestamp when this seed manifest was initially created."
    },
    "last_updated": {
      "$ref": "vc:core:atoms#timestamp",
      "description": "ISO 8601 timestamp when this seed manifest was last modified."
    },
    "global_seed_order": {
      "$ref": "vc:core:collections#kebabIdArray",
      "description": "Ordered array of seed IDs defining the default ingestion sequence across all pipeline steps."
    },
    "seeds": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "seed_id": {
            "$ref": "vc:core:atoms#kebabId",
            "description": "Unique kebab-case identifier for this seed document."
          },
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "File path to the seed document relative to the repository root."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable summary of what this seed document contains."
          },
          "required": {
            "type": "boolean",
            "description": "Whether this seed document must be present for pipeline execution."
          },
          "source_type": {
            "type": "string",
            "enum": [
              "doc",
              "spec",
              "config",
              "other"
            ],
            "description": "Category of the seed source: doc (documentation), spec (specification), config (configuration), or other."
          }
        },
        "required": [
          "seed_id",
          "path",
          "description",
          "required",
          "source_type"
        ],
        "description": "A seed document definition with path, source type, and required flag."
      },
      "description": "Array of seed document definitions, each describing a source document available for ingestion."
    },
    "step_requirements": {
      "type": "object",
      "patternProperties": {
        "^(\\d{2}[a-z]?)$": {
          "description": "Array of seed IDs required for this pipeline step.",
          "$ref": "vc:core:collections#kebabIdArray"
        }
      },
      "additionalProperties": false,
      "description": "Map from pipeline step numbers to arrays of seed IDs required for that step."
    },
    "doc_paths": {
      "description": "Glob patterns identifying documentation files in the host repository. Used by step_16 validator to verify that docs_impact entries reference valid doc paths.",
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true
    }
  },
  "required": [
    "seed_manifest_id",
    "version",
    "created_at",
    "last_updated",
    "global_seed_order",
    "seeds",
    "step_requirements"
  ],
  "description": "Schema for the seed manifest defining seed documents, their ordering, and step requirements."
}
