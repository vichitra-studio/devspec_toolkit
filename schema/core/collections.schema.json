{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:core:collections",
  "title": "Core Collections",
  "$defs": {
    "kebabIdArray": {
      "$anchor": "kebabIdArray",
      "type": "array",
      "description": "Non-empty array of unique kebab-case identifiers, used for cross-referencing spec artifacts.",
      "items": {
        "$ref": "vc:core:atoms#kebabId",
        "description": "A kebab-case identifier."
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "stringArray": {
      "$anchor": "stringArray",
      "type": "array",
      "description": "Generic array of strings used for lists of labels, paths, exclusions, or freeform text entries.",
      "items": {
        "type": "string",
        "description": "A string entry in the array."
      }
    },
    "link": {
      "$anchor": "link",
      "type": "object",
      "description": "Hyperlink object representing a relationship between artifacts, with an optional spec cross-reference.",
      "additionalProperties": false,
      "properties": {
        "rel": {
          "type": "string",
          "description": "Relationship type describing how the linked resource relates to this artifact (e.g., 'source', 'documentation')."
        },
        "href": {
          "type": "string",
          "description": "URL or relative path to the linked resource."
        },
        "spec_ref": {
          "$ref": "vc:core:atoms#kebabId",
          "description": "Optional kebab-case ID referencing another spec artifact related to this link."
        }
      },
      "required": [
        "rel",
        "href"
      ]
    },
    "traceId": {
      "$anchor": "traceId",
      "description": "Kebab-case identifier used as a traceability target, linking requirements to implementations.",
      "$ref": "vc:core:atoms#kebabId"
    },
    "traceRef": {
      "$anchor": "traceRef",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Trace type — validated against canon/kinds/trace_type.json entries."
        },
        "id": {
          "$ref": "vc:core:atoms#kebabId",
          "description": "Kebab-case ID of the target artifact being traced to."
        },
        "note": {
          "type": "string",
          "description": "Optional human-readable explanation of why this trace link exists."
        }
      },
      "required": [
        "type",
        "id"
      ],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "external"
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "required": [
              "owner",
              "note"
            ]
          }
        }
      ],
      "description": "Typed cross-reference linking one spec artifact to another for traceability. External traces require an owner and note."
    },
    "canonicalRef": {
      "$anchor": "canonicalRef",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^cn:[a-z0-9.]+:[a-z_]+:[a-z0-9-]+$",
          "description": "Fully qualified canonical ID in the format cn:<namespace>:<kind>:<slug>."
        },
        "kind": {
          "type": "string",
          "pattern": "^[a-z_][a-z0-9_]*$",
          "description": "Canonical kind category this reference belongs to (e.g., unit, stage, environment)."
        },
        "version": {
          "type": "string",
          "description": "Semantic version of the canonical entry at the time of reference."
        },
        "label": {
          "type": "string",
          "description": "Human-readable display label for this canonical reference."
        },
        "alias_used": {
          "type": "string",
          "description": "If an alias was used in the source, records the original alias string before resolution."
        },
        "note": {
          "type": "string",
          "description": "Optional note explaining why this canonical term was chosen."
        }
      },
      "required": [
        "id",
        "kind"
      ],
      "description": "Reference to a canonical registry entry, binding a spec field value to the shared vocabulary."
    },
    "canonicalRefArray": {
      "$anchor": "canonicalRefArray",
      "type": "array",
      "items": {
        "$ref": "vc:core:collections#canonicalRef",
        "description": "A canonical registry reference."
      },
      "description": "Array of canonical registry references used within an artifact."
    },
    "canonicalProposal": {
      "$anchor": "canonicalProposal",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "temp_id": {
          "$ref": "vc:core:atoms#kebabId",
          "description": "Temporary kebab-case ID assigned to this proposal until it is accepted into the registry."
        },
        "kind": {
          "type": "string",
          "description": "Canonical kind category for the proposed term (e.g., unit, entity, role)."
        },
        "proposed_label": {
          "type": "string",
          "description": "Human-readable label suggested for the new canonical entry."
        },
        "definition": {
          "type": "string",
          "description": "Clear definition of what this proposed canonical term represents."
        },
        "source_field": {
          "type": "string",
          "description": "JSON path or field name in the source artifact where this term was encountered."
        },
        "suggested_namespace": {
          "type": "string",
          "pattern": "^[a-z0-9.]+$",
          "description": "Dot-separated namespace suggestion for organizing this term in the canonical registry."
        }
      },
      "required": [
        "temp_id",
        "kind",
        "proposed_label",
        "definition",
        "source_field"
      ],
      "description": "Proposal for a new canonical term discovered during AI generation that does not yet exist in the registry."
    },
    "canonicalProposalArray": {
      "$anchor": "canonicalProposalArray",
      "type": "array",
      "items": {
        "$ref": "vc:core:collections#canonicalProposal",
        "description": "A proposed canonical term."
      },
      "description": "Array of proposed canonical terms discovered during artifact generation."
    },
    "canonicalConflict": {
      "$anchor": "canonicalConflict",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "field_path": {
          "type": "string",
          "description": "JSON path to the field in the artifact where the conflict was detected."
        },
        "input_value": {
          "type": "string",
          "description": "The original input value that triggered the ambiguous match."
        },
        "candidate_ids": {
          "$ref": "vc:core:collections#stringArray",
          "description": "List of canonical IDs that all matched the input value."
        },
        "reason": {
          "type": "string",
          "description": "Explanation of why the conflict could not be auto-resolved."
        }
      },
      "required": [
        "field_path",
        "input_value",
        "candidate_ids",
        "reason"
      ],
      "description": "Record of an ambiguous canonical resolution where multiple registry entries matched an input value."
    },
    "canonicalConflictArray": {
      "$anchor": "canonicalConflictArray",
      "type": "array",
      "items": {
        "$ref": "vc:core:collections#canonicalConflict",
        "description": "A canonical resolution conflict."
      },
      "description": "Array of canonical resolution conflicts encountered during artifact generation."
    },
    "environmentName": {
      "$anchor": "environmentName",
      "type": "string",
      "enum": [
        "dev",
        "ci",
        "staging",
        "prod"
      ],
      "description": "Deployment environment name. One of dev, ci, staging, or prod."
    },
    "stageName": {
      "$anchor": "stageName",
      "type": "string",
      "enum": [
        "dev",
        "ci",
        "staging",
        "prod"
      ],
      "description": "Pipeline stage name indicating the deployment target. One of dev, ci, staging, or prod."
    },
    "environmentConfig": {
      "$anchor": "environmentConfig",
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_-]*$",
        "description": "Property name constraint: must be lowercase alphanumeric with optional hyphens or underscores."
      },
      "additionalProperties": {
        "oneOf": [
          {
            "type": "string",
            "description": "String configuration value."
          },
          {
            "type": "number",
            "description": "Numeric configuration value."
          },
          {
            "type": "boolean",
            "description": "Boolean configuration value."
          },
          {
            "type": "array",
            "items": {
              "type": "string",
              "description": "A string entry in the configuration array."
            },
            "description": "Array of string configuration values."
          },
          {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "description": "A string value in the configuration map."
            },
            "description": "Object map of string configuration values."
          }
        ],
        "description": "Allowed value types for environment configuration entries."
      },
      "description": "Key-value configuration map for a deployment environment, supporting string, number, boolean, string array, and string-map values."
    },
    "techStackItem": {
      "$anchor": "techStackItem",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the technology (e.g., Python, React, PostgreSQL)."
        },
        "version": {
          "type": "string",
          "description": "Version string or constraint for this technology."
        },
        "notes": {
          "type": "string",
          "description": "Optional implementation notes about this technology choice."
        },
        "rationale": {
          "type": "string",
          "description": "Justification for selecting this technology over alternatives."
        },
        "tech_stack_ref": {
          "$ref": "vc:core:collections#canonicalRef",
          "description": "Optional canonical reference linking this tech stack item to the canonical registry."
        }
      },
      "required": [
        "name",
        "version"
      ],
      "description": "A single technology component in the project stack, with version and optional rationale."
    },
    "techStackList": {
      "$anchor": "techStackList",
      "type": "array",
      "items": {
        "$ref": "vc:core:collections#techStackItem",
        "description": "A technology stack item."
      },
      "description": "Array of technology stack items within a single category."
    },
    "techStack": {
      "$anchor": "techStack",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "languages": {
          "$ref": "vc:core:collections#techStackList",
          "description": "Programming languages used in the project."
        },
        "frameworks": {
          "$ref": "vc:core:collections#techStackList",
          "description": "Application frameworks and libraries used in the project."
        },
        "infrastructure": {
          "$ref": "vc:core:collections#techStackList",
          "description": "Infrastructure and platform services (databases, cloud providers, CI/CD)."
        },
        "tools": {
          "$ref": "vc:core:collections#techStackList",
          "description": "Development and operational tools (linters, bundlers, monitoring)."
        }
      },
      "required": [
        "languages",
        "frameworks"
      ],
      "description": "Structured technology stack organized by category: languages, frameworks, infrastructure, and tools."
    },
    "dependencyItem": {
      "$anchor": "dependencyItem",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "milestone",
            "external"
          ],
          "description": "Dependency type: milestone for internal project milestones, external for third-party or cross-team dependencies."
        },
        "id": {
          "$ref": "vc:core:atoms#kebabId",
          "description": "Kebab-case identifier for this dependency."
        },
        "owner": {
          "type": "string",
          "description": "Team or individual responsible for resolving this dependency. Required for external dependencies. Deliberately unconstrained (not the 8-value owner enum) because external dependency owners are free-form team names (e.g., 'platform-team', 'security-team') that may not map to canonical artifact-owner roles."
        },
        "note": {
          "type": "string",
          "pattern": "^\\S+\\s+\\S+.*$",
          "description": "Description of the dependency with at least two words. Required for external dependencies."
        },
        "dependency_ref": {
          "$ref": "vc:core:collections#canonicalRef",
          "description": "Optional canonical reference linking this dependency to the canonical registry."
        }
      },
      "required": [
        "type",
        "id"
      ],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "external"
              }
            },
            "required": [
              "type"
            ]
          },
          "then": {
            "required": [
              "owner",
              "note"
            ]
          }
        }
      ],
      "description": "A milestone or external dependency that must be resolved before work can proceed."
    },
    "dependencyList": {
      "$anchor": "dependencyList",
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "string",
            "description": "A simple string dependency identifier."
          },
          {
            "$ref": "vc:core:collections#dependencyItem",
            "description": "A structured dependency item with type, ID, and optional owner."
          }
        ],
        "description": "A dependency entry: either a simple string ID or a structured dependency item."
      },
      "description": "Array of dependencies, each either a simple string ID or a structured dependency item."
    },
    "dependencyObjectList": {
      "$anchor": "dependencyObjectList",
      "type": "array",
      "items": {
        "$ref": "vc:core:collections#dependencyItem",
        "description": "A structured dependency item."
      },
      "description": "Array of structured dependency items (no simple string entries allowed)."
    },
    "anyJson": {
      "$anchor": "anyJson",
      "oneOf": [
        {
          "type": "object",
          "description": "JSON object value."
        },
        {
          "type": "array",
          "description": "JSON array value."
        },
        {
          "type": "string",
          "description": "JSON string value."
        },
        {
          "type": "number",
          "description": "JSON number value."
        },
        {
          "type": "integer",
          "description": "JSON integer value."
        },
        {
          "type": "boolean",
          "description": "JSON boolean value."
        },
        {
          "type": "null",
          "description": "JSON null value."
        }
      ],
      "description": "Accepts any valid JSON value (object, array, string, number, integer, boolean, or null)."
    }
  }
}
