{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:canon:kind",
  "title": "Canonical Kind Registry",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "JSON Schema URI identifying the schema this kind registry file conforms to."
    },
    "registry_version": {
      "$ref": "vc:core:canon#/$defs/semver",
      "description": "Semantic version of the canonical registry format for this kind file."
    },
    "kind": {
      "type": "string",
      "pattern": "^[a-z_][a-z0-9_]*$",
      "description": "Lowercase identifier for the canonical kind category defined in this file (e.g., unit, owner, stage)."
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "vc:core:canon#/$defs/entry"
      },
      "description": "Array of canonical entries belonging to this kind, each validated against the core canon entry schema."
    }
  },
  "required": [
    "registry_version",
    "kind",
    "entries"
  ]
}
