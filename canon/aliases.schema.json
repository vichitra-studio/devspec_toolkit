{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vc:canon:aliases",
  "title": "Canonical Alias Registry",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "JSON Schema URI identifying the schema this aliases registry file conforms to."
    },
    "registry_version": {
      "$ref": "vc:core:canon#/$defs/semver",
      "description": "Semantic version of the canonical registry format for the aliases file."
    },
    "aliases": {
      "type": "array",
      "items": {
        "$ref": "vc:core:canon#/$defs/alias"
      },
      "description": "Array of alias mappings, each resolving an alternative name to a canonical entry ID."
    }
  },
  "required": [
    "registry_version",
    "aliases"
  ]
}
