# Archive

This directory contains historical audit documents produced during the toolkit's structured review cycles (r1–r9). These are kept for reference only and may not reflect the current state of the codebase.

For the current state of the codebase, refer to the live spec files in `spec/`, the validation suite in `tests/`, and the developer docs in `docs/developers/`.
